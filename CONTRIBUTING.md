# Contributing

1. Build following the [build guide](docs/build_guide/README.md).
2. Make your change.
3. Run `make installcheck` and `make prove_installcheck` and ensure they pass.
4. We recommend running `pgindent` on changed C files before submitting.
5. Open a PR using the provided template.

See [security.md](security.md) for reporting vulnerabilities.
